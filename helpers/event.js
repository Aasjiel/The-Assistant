const client = require("../index.js");


// exports.startEvent = (id, guildID) => {
//   const guild = client.guilds.cache.get(guildID);
//   // console.log(guild);
//   const eventById = guild.scheduledEvents.cache.find((e) => e.id === id);
//   eventById.setStatus("ACTIVE");
// };
